/*
import { JsonDocRaw } from "../i-doc/json-doc-raw";
import { DocsModel } from "../docs-model";
import { parseDocs } from "./parse-docs";

function formatCodeSnippets(
        //$sce: angular.ISCEService,
        $timeout  : angular.ITimeoutService,
        /*jsonDocRaw: JsonDocRaw, 
        model: DocsModel*//*) {

    $timeout().then(() => {
        let hljs = (window as any).hljs as any;
        let els;
        els = document.getElementsByClassName('javascript');
        for (let i=0; i<els.length; i++) {
            hljs.highlightBlock(els.item(i));
        }
        els = document.getElementsByClassName('html');
        for (let i=0; i<els.length; i++) {
            hljs.highlightBlock(els.item(i));
        }
        els = document.getElementsByClassName('shell');
        for (let i=0; i<els.length; i++) {
            hljs.highlightBlock(els.item(i));
        }
    });
}


export { formatCodeSnippets }
*/